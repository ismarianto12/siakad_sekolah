import 'package:flutter/material.dart';

class Menu {
  String title;
  String url;
  IconData icon;
  Color colors;
  Menu(
      {required this.title,
      required this.url,
      required this.icon,
      required this.colors});
}

// List data = ['Data Siswa', 'data_siswa', Icons.user];
